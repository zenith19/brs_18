<h1><%= t "all_books" %></h1>

<table class="table">
  <thead>
    <th></th>
    <th><%= t "title" %></th>
    <th><%= t "author" %></th>
    <th><%= t "publish_date" %></th>
    <th><%= t "page" %></th>
  </thead>
  <tbody>
    <% @books.each do |book|%>
      <tr>
        <td><%= image_tag book.book_photos.first.image_url(:thumb).to_s %></td>
        <td><%= link_to book.title, "#" %></td>
        <td><%= book.author %></td>
        <td><%= l book.publish_date, format: :short %></td>
        <td><%= book.page %></td>
        <td align="right">
          <%= link_to t(:edit), edit_admin_book_path(book),
            class: "btn btn-primary" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @books %>
<%= page_entries_info @books %>
