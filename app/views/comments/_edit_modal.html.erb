<%= form_for comment, html: {role: :form, "data-model": "comment"}, remote: true do |f| %>
  <%= render "shared/error_messages", object: f.object %>

  <%= f.text_field :content, class: "form-control" %>

  <%= f.submit t(:save_change_text), class: "btn btn-primary" %>
<% end %>
