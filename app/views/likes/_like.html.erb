<%= form_for Like.new, remote: true do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :activity_id, value: activity.id %>
  <%= f.submit t(:like), class: "btn btn-primary", id: "like-btn" %>
<% end %>